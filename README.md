# React Advanced. Финальный проект

## Что сделано

### 1. Архитектура и структура

- [x] Вынести UI-компоненты (например, Button, Input, Loader) в src/shared/ui.

- [x] Разделить фичи по папкам — например, src/features/cart/, src/features/auth/, src/features/search/.

- [x] Убрать лишние проп-пробросы, где можно использовать селекторы из RTK Query или Redux.

- [x] Проанализировать проект и найти часто встречающиеся UI-компоненты (Button, Input, Loader). Вынесите эти компоненты в src/shared/ui и использовать их в дальнейшем при разработке

#### Архитектура и структура: Чек-лист

- [x] В shared/ui лежат только презентационные (UI) компоненты:

  - [x] без бизнес-логики,

  - [x] без работы с API,

  - [x] без доступа к стору,

  - [x] принимают данные и коллбэки только через props..

- [x] Фичи изолированы в отдельных папках.

- [x] Нет глубоких prop-chain’ов без необходимости.

### 2. Оптимизация рендеров

- [X] Найти дубли рендеров через React DevTools Profiler.

  - [x] Обернуть компоненты, получающие неизменные props, в React.memo.

  - [x] В списках (например, товаров или корзины) применить useMemo/useCallback там, где есть вычисления или колбэки.
  
#### Оптимизация рендеров: Чек-лист

- [x] Использован Profiler — найден hotspot.

- [x] React.memo применяется там, где эффективно.

- [x] useMemo/useCallback оптимизируют сложные вычисления/функции.

### 3. React.Portal для модалок

- [x] В public/index.html добавить `<div id="modal-root"></div>`.

- [x] Реализовать компонент Modal в src/shared/ui/Modal через ReactDOM.createPortal.

#### React.Portal для модалок: Чек-лист

- [x] Модальное окно работает через портал.

- [x] Закрытие модального окна через клик по иконке крестика, клику вне области модалки и нажатие на клавишу ESC корректно обрабатываются

- [x] Фокус обрабатывается корректно:

- [x] при открытии модального окна начальный фокус устанавливается на кнопку закрытия модалки (иконку крестика),  

- [ ] при закрытии модального окна — фокус возвращается на элемент-триггер.

### 4. useRef — реальное применение

- [x] Использовать useRef для хранения состояния между рендерами без перерендера — например, счётчик кликов или таймер.

- [x] Автофокус на инпут формы (регистрации/логина) при монтировании через ref.

#### useRef: Чек-лист

- [x] Пример useRef без рендера.

- [x] Автофокус или другое взаимодействие с DOM.

### 5. Собственные сборки (esbuild/swc)

- [x] Создать альтернативный сборочный конфиг: например, esbuild.config.js или vite.config.js с SWC.

- [x] Сравнить build time и output-size с Webpack, записать результаты.

#### Собственные сборки: Чек-лист

- [x] Сборка через esbuild или SWC работает.

- [x] В README приведено сравнение (размер, время).

### 6. Применение React 19 Hooks

- [ ] В одной форме (например, комментарий) использовать useActionState для отправки и автоматического reset.

- [x] Или useOptimistic для мгновенных UI-реакций (например, добавление товара в избранное).

- [x] Где: например, src/features/productDetails/AddCommentForm.jsx или CartItem.

- [x] Можно использовать любой хук из тех, что появились в 19 реакте, однако необходимо обосновать его применение на защите

### Применение React 19 Hooks: Чек-лист

- [x] Использован useActionState или useOptimistic.

- [x] UI реагирует до ответа сервера (оптимистично).

## Сравнение сборок

| Сборщик     | Размер (KiB) | Время сборки (с) |
| :---------- | -----------: | ---------------: |
| Webpack     |          599 |             11.3 |
| Vite+Rollup |          204 |              1.2 |
